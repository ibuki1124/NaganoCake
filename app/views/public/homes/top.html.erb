<div class="row">
  <div class="col-md-3">
    <table class="table table-bordered mb-0">
      <thead>
        <th>ジャンル検索</th>
      </thead>
      <tbody>
        <% @genres.each do |genre| %>
          <tr>
            <td><%= link_to "#" + genre.name, items_path(genre_name: genre.name) %></td>
          </tr>
        <% end %>
    </table>
    <%= paginate @genres, :param_name => 'genre_page' %>
  </div>
  <div class="col-md-9">
    <div class="d-flex justify-content-end top-image px-2 py-5 img-fluid">
      <div class="text-light bg-dark p-1 p-md-3" style="opacity: .6;">
        ようこそ、ながのCAKEへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。
      </div>
    </div>
    <h2 class="my-3">新着商品</h2>
    <div class="row">
      <% @items.each do |item| %>
        <% if item.is_active %>
          <div class="col-md-3 col-6">
            <%= link_to item_path(item) do %>
              <%= image_tag item.image, class:"img-fluid" %>
            <% end %>
            <p class="font-weight-bold"><%= item.name %></p>
            <p><%= number_to_currency(item.with_tax_price, unit: "￥", format: "%u%n") %></p>
          </div>
        <% end %>
      <% end %>
    </div>
    <div class="text-right">
      <%= link_to "全ての商品を見る", items_path %>
    </div>
  </div>
</div>